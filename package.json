{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "THIs is the node server for i chat app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^17.0.0",
    "nodemon": "^2.0.6",
    "socket.io": "^2.3.0"
  }
}
